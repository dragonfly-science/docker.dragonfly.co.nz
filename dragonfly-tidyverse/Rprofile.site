# Set the URL for CRAN
local({
    r <- getOption('repos')
    r['CRAN'] = 'https://cran.microsoft.com/snapshot/2018-01-17/'
    options(repos = r)
})
.libPaths('/usr/local/lib/R/site-library')

# And use A4 paper
options(papersize="a4") 

